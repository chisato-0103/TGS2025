using UnityEngine;

public class ketu : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("ケツ");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
